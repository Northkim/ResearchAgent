# Owner Experiment scaffold interactive bootstrap repair

Date: 2026-08-13

The owner reached the correct Reproduction & Experiment Capsule through exact
Artifact binding, materialization, and the generic Workspace launcher, but the
published Capsule 0.3.0 invoked bare `codex` and supplied no initial user turn.
AGENT/AGENTS discovery supplied standing instructions only; it could not begin
the Workflow. Earlier F1B/F1F qualification called scaffold finalizers directly
and Playwright inspected browser state, so neither exercised a fresh
interactive Experiment Harness.

The repair keeps Workflow Definition 0.3.0 and every scaffold research,
Artifact, Progress, Skill, and Resource contract unchanged. Historical Capsule
0.3.0 is byte- and checksum-immutable. Seed-only migration `20260813_0019`
publishes Capsule 0.4.0, which adds a bounded ReAgent-owned positional Codex
prompt and a credential-free local Resource-status projection. New Instances
resolve 0.4.0; existing 0.3.0 Instances remain pinned.

The automatic first turn begins `REAGENT REPRODUCTION & EXPERIMENT —
INPUT_REVIEW`, directs the Harness to the exact versioned local contracts,
required selected Idea and optional selected Literature Artifact, reports the
four optional Resource categories accurately, and states all frozen safety
limits. It cannot execute Resource bytes or claim simulation, training,
metrics, paper reproduction, or scientific results. Finalization still enforces
`PLACEHOLDER_NOT_EXECUTED` and `actual_results = null`.

Qualification used only disposable Project/database/Workspace state and no
OpenAlex call. The exact Workspace-root command with a Unicode/space path and a
PTY-enforcing fake Harness automatically showed INPUT_REVIEW, published the
unchanged experiment-record/v1 safety shape, and uploaded COMPLETED Progress.
A bounded real Codex CLI run against a synthetic selected Idea visibly received
the ReAgent positional prompt before being safely exited; it reported zero
Provider calls, zero Resource execution, and no owner data.

Final qualification: focused bootstrap/version/product tests passed; full
backend on a generated disposable PostgreSQL database passed `819 passed, 14
existing skips`; frontend Vitest passed `17 files / 34 tests`, TypeScript,
ESLint and production build passed; controlled fake-provider Playwright passed
`4`; owner-runtime isolation/restart passed; compileall, Alembic sole head and
schema check, and git diff check passed. Every generated database was dropped.

Owner continuation stays in the same Project: migrate/restart, replace only the
downloaded Workspace-root client, retire the old Experiment 0.3.0 Instance, add
a new Experiment Instance, bind the existing same-Project selected Idea and any
optional Literature Artifact, sync, materialize, and run. Literature and Idea
must not be rerun. Do not begin Real Experiment Core automatically.

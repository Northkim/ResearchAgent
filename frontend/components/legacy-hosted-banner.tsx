export function LegacyHostedBanner() {
  return (
    <div className="legacy-banner" role="note">
      <strong>Legacy Hosted Mode — not part of V0.1</strong>
      <p>This preserved internal prototype can execute server-side Workflow runs. The supported V0.1 product starts at Projects and runs research locally with Codex.</p>
    </div>
  );
}
